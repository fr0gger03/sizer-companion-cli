[project]
name = "sizer-companion-cli"
version = "1.0.0"
description = "A python-based cli to help prepare data for use with VMware sizing tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.1",
    "rich>=15.0.0",
]
